# Base packages
Flask==2.2.2
click==8.1.3
itsdangerous==2.1.2
MarkupSafe==2.1.1
Werkzeug==3.1.5

# Known vulnerable packages for security testing
# CVE-2021-41040 - urllib3 authorization header vulnerability
urllib3==1.24.3

# CVE-2020-1747 - PyYAML arbitrary command execution
PyYAML==5.3.1

# CVE-2021-3572 - pip vulnerability (will be installed)
# Note: Can't pin old pip in requirements.txt, but base image has vulnerable version

# CVE-2020-12888 - cryptography vulnerability
cryptography==3.1

# CVE-2020-5902 - requests vulnerability (older version)
requests==2.25.1

# CVE-2019-14806 - Werkzeug path traversal (older version)
# Using current but demonstrating version tracking
six==1.15.0
